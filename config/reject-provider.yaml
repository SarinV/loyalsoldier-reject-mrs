# Replace <OWNER>/<REPO> after creating the public GitHub repository.
rule-providers:
  reject:
    type: http
    behavior: domain
    format: mrs
    interval: 86400
    url: https://raw.githubusercontent.com/<OWNER>/<REPO>/release/reject.mrs
    path: ./ruleset/loyalsoldier/reject.mrs
